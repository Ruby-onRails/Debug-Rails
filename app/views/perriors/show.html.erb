<div class="container mx-auto my-8 px-4">
    <p class="text-lg font-semibold">Title:</p>
  <p class="leading-normal"><%= @perrior.title %></p>

    <p class="text-lg font-semibold">Content:</p>
  <p class="leading-normal"><%= @perrior.content %></p>

    <div class="flex items-center justify-start mt-6">
    <%= link_to 'Edit', edit_perrior_path(@perrior), class: "btn btn-default mr-4" %>
    <%= link_to 'Back', perriors_path, class: "btn btn-default" %>
  </div>
</div>